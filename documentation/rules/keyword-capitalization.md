# Requires use of capitalized keywords

## Rule Details

Capitalizing SQL keywords enhances readability and provides a clear seperation between keywords and objects

```sql
select * from foo;
```

Examples of **correct** code for this rule:

```sql
SELECT * FROM foo;
```
